name: 'normanfleet'
version: '1.0.0'
profile: 'normanfleet'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  normanfleet:
    staging:
      +materialized: view
    marts:
      +materialized: table
